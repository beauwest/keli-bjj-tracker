BeauWest.BJJ_Tracker.Technique,
  launchpad,
  menu,
  menuCaption 'BJJ Techniques',
  icon 'head-sync',
  caption(_browse) 'BJJ Techniques',
  caption(_edit) 'BJJ Technique'

Name

Hours,
  int,
  c (SELECT SUM(Minutes) / 60 FROM Session WHERE Technique = NEW.Id)